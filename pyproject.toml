[tool.ruff.lint]
select = [
    "D",
    "E",
    "F",
    "UP",
    "B",
    "SIM",
    "I",
]
[tool.coverage.run]
source = ["."]
omit = [
    "/tests/",
    "/venv/",
]

[tool.pytest.ini_options]
markers = [
    "perf: Tests de performance (a exclure des tests unitaires)",
]